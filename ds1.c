#include<stdio.h>
string reveerse(string s)
	{
	    if(s.length()==0)
	    return "";
	    string s1=reveerse(s.substr(1))+s;
	    return s1;
	}
int main()
{
    string s="abs";
    cout<<reerse
    printf("%d",show(15,5));
}
